SELECT p.product_id, p.product_name
FROM Product AS p 
INNER JOIN Sales AS s ON p.product_id = s.product_id 
GROUP BY p.product_id 
HAVING COUNT(p.product_id) = SUM(sale_date BETWEEN "2019-01-01" AND “2019-03-31");
